import { PrismaClient } from "@prisma/client";

export const resolvers = {

  Query: {
    // createUser: async (_: any, __: any, { prisma }: { prisma: PrismaClient }) => {
      
      
    // }
  },
  Mutation: {

  }
}